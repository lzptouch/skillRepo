# <能力名称> · 流程契约

> 粒度自检：换个技术栈实现，这份文档还应该成立。若不成立，说明你把 HOW 混进了 WHAT —— 把技术细节挪到 `implementation-notes.md`。

## Problem（解决什么）

<!-- 一句话说清这个能力解决什么问题。 -->

## Non-Goals（明确不做什么）

<!-- 这一栏最重要。AI 的过度设计几乎都发生在没有边界的地方。 -->

- 不处理：
- 不支持：
- 不负责：

## Preconditions（前置条件）

| 条件 | 必需性 | 缺失时行为 |
|---|---|---|
| 环境变量 / 配置 | 必需 | 启动时报错并指明缺哪个 |
| 外部服务可用 | 必需 | 显式失败，禁止降级静默通过 |
| 数据前提 | 可选 | 说明降级行为 |

## Steps（执行顺序）

| # | 步骤 | 输入 | 输出 | 失败时 |
|---|---|---|---|---|
| 1 | | | | |
| 2 | | | | |
| 3 | | | | |

<!-- 每步写清楚落在哪个文件哪几行。 -->

## Interface（接口）

```
输入：<type> <name>  —— 约束（必填？范围？格式？）
输出：<type>         —— 成功时
异常：<type>         —— 什么时候抛
副作用：写库 / 发消息 / 计费 —— 有则写明
```

## Failure Modes（失败模式）

| 失败 | 检测方式 | 处理 | 错误信息 |
|---|---|---|---|
| | | | |

<!-- 硬性要求：禁止吞异常。每个 catch 要么记录原因重试，要么转成明确错误类型抛出。 -->

## Acceptance（验收标准，必须可测试）

- [ ] 成功路径：给定 X，必然得到 Y
- [ ] 边界：<具体数值/场景> 下的行为
- [ ] 失败：输入非法时返回明确错误，不返回 null / undefined
- [ ] 性能：<具体指标>，例如 P95 < 50ms

<!-- 「ui 要好用」不是验收标准。「P95 < 50ms」才是。 -->

## Hallucination 风险点

<!-- AI 实现这个流程时最容易做错的地方，会直接变成 pbs-audit-module 的检查项。 -->

- 
- 

## Constraints（外部约束）

- 必须使用 / 禁止使用某组件
- 兼容要求
- 合规要求
